# Docker uses the same external H2 instance model as local execution.
spring.datasource.url=jdbc:h2:file:${CONFERIA_DB_PATH:/app/instance/conferia}
file.upload-dir=${CONFERIA_UPLOAD_DIR:/app/instance/uploads}
